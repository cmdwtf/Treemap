namespace cmdwtf.Treemap.Tiling
{
	/// <summary>
	/// An enum representing which orientation the current treemap node should be laying out.
	/// This is used internally to layout the node boxes.
	/// </summary>
	internal enum RowOrientation
	{
		Horizontal,
		Vertical
	}
}
